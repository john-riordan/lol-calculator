<script>
	import { championData, itemsData } from '$lib/stores';

	// export let data;

	// $: {
	// 	if (data.champions) {
	// 		championData.set(data.champions);
	// 	}
	// 	if (data.items) {
	// 		itemsData.set(data.items);
	// 	}
	// }
</script>

<slot />
